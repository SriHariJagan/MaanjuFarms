import React from "react";

const Loader = () => {
  return (
    <div
      style={{
        height: "70vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: "18px",
        fontWeight: "500",
      }}
    >
      Loading...
    </div>
  );
};

export default Loader;
